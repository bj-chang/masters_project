"""Mesh-dependence in PDE-constrained optimisation.

Submodules: ``pdeopt.problems`` (model problems), ``pdeopt.scripts``
(experiment runners), ``pdeopt.meshes`` (mesh constructors),
``pdeopt.optimisers`` (optimisation wrappers).
"""
